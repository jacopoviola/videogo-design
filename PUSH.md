# Push the VIDEOGO design system to jacopoviola/videogo-design

Unzip `videogo-design-system.zip` into a clone of the repo (files land at the repo root, beside `videogo-tokens_1.css`), then:

```bash
cd videogo-design
git add -A
git commit -m "design system: tokens, components, artboards, brand mark, screens, brandbook" \
  --author="Jacopo Viola <jacopoviola123@gmail.com>"
git push origin main
```

95 files · 207,127 bytes.
