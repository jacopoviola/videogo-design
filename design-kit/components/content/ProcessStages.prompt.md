Observable process, opaque method: discrete working stages with a bone check for done and a filled square for the current one — never a progress bar for an unknown duration.
<ProcessStages stages={['Understanding your business', 'Finding opportunities', 'Building the video', 'Checking consistency', 'Ready']} current={2} note="usually a few minutes" />
Use in onboarding and Studio. Stage names are the fixed public vocabulary; never expose model names, prompts or scores.
