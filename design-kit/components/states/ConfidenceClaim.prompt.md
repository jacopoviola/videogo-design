Confidence ladder: dashed (collecting) → solid hairline (early signal, names the next test) → solid border + panel (learned, with evidence rows).
<ConfidenceClaim grade="learned" statement="Question-led videos get saved about 3× more."><EvidenceRow rows={[{ k: 'Evidence', v: '12 videos over 8 weeks' }]} /></ConfidenceClaim>
Never a percentage or meter. A claim may only move up a grade.
