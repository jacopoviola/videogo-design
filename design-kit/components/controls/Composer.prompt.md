The persistent secondary strip at the bottom of a section; ghost Attach, field, one primary Send.
<Composer onSend={t => console.log(t)} />
Never the page itself; a reply appears as an object with an Attribution, never as a chat transcript.
