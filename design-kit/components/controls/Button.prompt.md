Three buttons only: blocking (bone fill, only inside a NEED), primary (amber, one per object), ghost (border #37322B, everything else).
<Button variant="primary">Publish</Button> <Button>Later</Button> <Button variant="blocking">Fix connection</Button>
34px desktop, touch → 44px. Hover/press change colour in 120ms; nothing moves. Never an icon-only button.
